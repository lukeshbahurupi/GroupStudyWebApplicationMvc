@model GroupStudyWebApplicationMvc.Models.Employee

<table>
    <tr>
        <th>Name : </th><td>@Model.Name</td>
    </tr>
    <tr>
        <th>Gemder : </th><td>@Model.Gender</td>
    </tr>
    <tr>
        <th>City : </th><td>@Model.City</td>
    </tr>
    <tr >
        <th>Salary : </th><td>@Model.Salary</td>
    </tr>
</table>
    @*<button class="text-decoration-none"><a href="/Employee/Index/@Model.DepartmentId">Back to List</a></button>*@
    @Html.ActionLink("Back to List", "Index", "Employee", new {id = Model.DepartmentId }, new { @class = "btn  m-2 bg-dark text-light" })

